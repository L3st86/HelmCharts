apiVersion: v1
kind: Service
metadata:
  name: {{ include "mosquito-mqtt.fullname" . }}
  labels:
    {{- include "mosquito-mqtt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mosquito-mqtt.selectorLabels" . | nindent 4 }}
