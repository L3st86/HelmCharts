apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mosquito-mqtt.fullname" . }}-test-connection"
  labels:
    {{- include "mosquito-mqtt.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mosquito-mqtt.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
