apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "UptimeKuma.fullname" . }}-test-connection"
  labels:
    {{- include "UptimeKuma.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "UptimeKuma.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
